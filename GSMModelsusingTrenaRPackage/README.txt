This was originally run in Pauls TRENAProjectPlacenta R Package.

Located ~/TrenaProjectPlacenta/ on Khaleesi

Paul Shannon had written this package to perform genome scale models.

We used this to provide the scaffolding of TF-Target genes using data from motifDB as a work around

Analysis performed July-November of 2019
